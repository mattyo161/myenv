# Managed by Ansible (roles/dotfiles) — edit group_vars/all.yml, not this file.
sourceDir = "{{ playbook_dir }}/dotfiles"

[data]
name = "{{ git_user_name }}"
email = "{{ git_user_email }}"
github_user = "{{ github_user }}"
